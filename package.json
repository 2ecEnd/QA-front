{
  "name": "recipe-book-frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test:ui": "npx playwright test --config=e2e-tests/playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "typescript": "^5.3.0"
  }
}
